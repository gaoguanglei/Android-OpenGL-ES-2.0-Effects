#ifdef GL_FRAGMENT_PRECISION_HIGH
   precision highp float;
#else
   precision mediump float;
#endif

uniform samplerCube EnvironmentMap;

varying vec3 verDirection;

void main(void)
{
   gl_FragColor = textureCube(EnvironmentMap,verDirection);
}